{
  "name": "weconverge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WEConverge — OMP AI-native scheduling policy layer (user-level Extension)",
  "scripts": {
    "test": "node --experimental-strip-types --loader ./scripts/node-ts-loader.mjs test/mechanical.test.ts",
    "test:native": "node --experimental-strip-types --loader ./scripts/node-ts-loader.mjs test/mechanical.test.ts",
    "check:ledger": "node scripts/check-ledger.mjs",
    "check:types": "node scripts/sync-omp-types.mjs",
    "typecheck:core": "npx tsc --noEmit -p tsconfig.core.json && npx tsc --noEmit -p tsconfig.extension.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
